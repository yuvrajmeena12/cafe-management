# Supabase
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Razorpay (public key only goes in frontend; secret key lives in the edge function)
VITE_RAZORPAY_KEY_ID=rzp_test_xxxxxxxx

# Note: Delivery address map + autocomplete uses OpenStreetMap (Leaflet +
# Nominatim) — completely free, no API key or billing account required.
# No Google Maps key needed anywhere in this project.
